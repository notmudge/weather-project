name: dbt_weather
version: "1.0.0"
profile: dbt_weather

model-paths: ["models"]
test-paths: ["tests"]

models:
  dbt_weather:
    silver:
      +schema: silver
      +materialized: incremental
    gold:
      +schema: gold
      +materialized: table
